/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        unoptimized: true, // Allow generic img tags for simplicity with external URLs
    },
};

module.exports = nextConfig;
